# Using NTPU API

The root endpoint of NTPU api is: `https://api.ntpu.cc/`

All endpoint we provided need an authorization token in request header. You can get the access token by follow this [instruction](/oauth#5-your--app-access-users-data-from-our-api-with-the-token-we-provide).

